{
  "name": "smart-ads-simulator",
  "private": true,
  "type": "module",
  "license": "GPL-3.0-only",
  "packageManager": "pnpm@10.23.0",
  "scripts": {
    "dev": "vite --mode localhost --host 127.0.0.1",
    "build": "vite build --mode production && node scripts/generate-seo.mjs production",
    "build:local": "vite build --mode localhost && node scripts/generate-seo.mjs localhost",
    "build:github": "vite build --mode github && node scripts/generate-seo.mjs github && node scripts/verify-github-pages-build.mjs",
    "build:cloudflare": "vite build --mode cloudflare && node scripts/generate-seo.mjs cloudflare && node scripts/prune-cloudflare-assets.mjs",
    "preview": "vite preview --mode localhost --host 127.0.0.1",
    "preview:local": "vite preview --mode localhost --host 127.0.0.1",
    "preview:github": "vite preview --host 127.0.0.1 --port 4177",
    "seo:generate": "node scripts/generate-seo.mjs production",
    "serve:github": "npm run build:github && npm run preview:github"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.2.1",
    "@vitejs/plugin-react": "^5.0.4",
    "framer-motion": "^12.38.0",
    "lucide-react": "^1.14.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "tailwindcss": "^4.2.1",
    "vite": "^7.3.1",
    "zustand": "^5.0.13"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "typescript": "^5.8.3"
  }
}
